import { DefalutTheme } from "styled-components";

export const lightTheme:DefalutTheme = {
    bgColor :"white",
    textColor : "black",
    btnColor : "tomato",
}

export const darkTheme:DefalutTheme = {
    bgColor : "black",
    textColor : "white",
    btnColor : "teal",
}